[LAB]
name = City Diagnostic Laboratory
phone = +92-300-1234567
address = Main Boulevard, Lahore, Pakistan
email = info@citydiagnostics.pk

[APP]
auto_start_listeners = yes
start_minimized = no

[DATABASE]
# Our app database (results, reports)
path = lab_data.db

# Client's existing database (read patient data from here)
# Supported: sqlite, mysql, postgresql, mssql (SQL Server), access
[CLIENT_DB]
enabled = yes
type = sqlite
host =
port =
name = brother_hospital.db
user =
password =
# SQL query — adjust column names to match client's table
patient_query = SELECT patient_id as mrn, patient_name as name, gender, date_of_birth as dob, contact_no as phone FROM tbl_patients
sync_interval = 1

[MACHINE_1]
name = Hematology Analyzer
port = COM3
baud_rate = 9600
enabled = yes

[MACHINE_2]
name = Chemistry Analyzer
port = COM4
baud_rate = 9600
enabled = no

[MACHINE_3]
name = Immunoassay Analyzer
port = COM5
baud_rate = 9600
enabled = no

[REPORT]
output_folder = reports
show_qr = yes
logo_path =
